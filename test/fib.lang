var start = time();

fn fibonacci(n) {
    if n <= 1 {
        return n;
    }

    return fibonacci(n - 2) + fibonacci(n - 1);
}

fn times(n, f) {
    var i = 0;
    while i < n {
        f(i);
        i = i + 1;
    }
}

times(20, fn (i) {
    print fibonacci(i);
});

print "";
print "Time in ms: " + (time() - start);
